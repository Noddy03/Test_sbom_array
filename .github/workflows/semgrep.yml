name: Security Scan – Trivy SCA + SBOM + OSV-Scanner + Licenses

on:
  workflow_dispatch:
    inputs:
      language:
        description: 'Choose language to scan'
        required: true
        default: 'Java'
      top_n:
        description: 'Number of repositories to scan'
        required: false
        default: '5'

permissions:
  contents: read
  security-events: write

jobs:
  # =========================================================
  # FETCH REPOSITORIES
  # =========================================================
  fetch_repos:
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.fetch.outputs.repos }}
    timeout-minutes: 10
    steps:
      - id: fetch
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LANG: ${{ inputs.language }}
          TOP_N: ${{ inputs.top_n }}
        run: |
          python3 <<'EOF'
          import os, requests, json, urllib.parse, time
          headers = {"Authorization": f"token {os.environ['TOKEN']}", "Accept": "application/vnd.github+json"}
          repos = []
          page = 1
          while len(repos) < int(os.environ["TOP_N"]) and page <= 10:
              url = f"https://api.github.com/search/repositories?q=language:{urllib.parse.quote(os.environ['LANG'])}&sort=stars&order=desc&per_page=30&page={page}"
              r = requests.get(url, headers=headers)
              if r.status_code != 200:
                  break
              for repo in r.json().get("items", []):
                  files = []
                  if os.environ["LANG"].lower() == "java":
                      files = ["pom.xml"]
                  elif os.environ["LANG"].lower() == "python":
                      files = ["requirements.txt", "pyproject.toml", "setup.py"]
                  found = False
                  for f in files:
                      if requests.get(
                          f"https://api.github.com/repos/{repo['full_name']}/contents/{f}",
                          headers=headers
                      ).status_code == 200:
                          found = True
                          break
                  if found:
                      repos.append(repo["full_name"])
                      if len(repos) >= int(os.environ["TOP_N"]):
                          break
              page += 1
              time.sleep(1)
          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write(f"repos={json.dumps(repos)}\n")
          EOF
  # =========================================================
  # SCAN EACH REPOSITORY
  # =========================================================
  scan_repo:
    needs: fetch_repos
    if: needs.fetch_repos.outputs.repos != '[]'
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        repo: ${{ fromJSON(needs.fetch_repos.outputs.repos) }}
    steps:
      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y jq git curl unzip openjdk-17-jdk maven python3-pip golang
          pip3 install --upgrade pip
          curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin
          # Install OSV-Scanner
          go install github.com/google/osv-scanner/cmd/osv-scanner@v1.9.0
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH
      - name: Clone repo
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: git clone --depth 1 https://x-access-token:${TOKEN}@github.com/${{ matrix.repo }} project

      # ---------------- JAVA ----------------
      - name: Build Java project
        if: ${{ inputs.language == 'Java' }}
        run: |
          cd project
          mvn -B -DskipTests package || true

      # ---------------- PYTHON ----------------
      - name: Build Python environment
        if: ${{ inputs.language == 'Python' }}
        run: |
          mkdir -p results
          cd project
          python3 -m venv .venv
          source .venv/bin/activate
          pip install --upgrade pip setuptools wheel
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          elif [ -f pyproject.toml ]; then
            pip install .
          fi
          pip freeze > ../results/requirements.lock.txt
          deactivate



      # ---------------- SCA - Trivy ----------------
      - name: Trivy Dependency SCA
        run: |
          mkdir -p results
          JARS=$(find project -type f -path "*/target/*.jar" 2>/dev/null)
          if [ -n "$JARS" ]; then
            trivy fs project --scanners vuln --format json \
              --ignore-unfixed --output results/trivy_sca.json || \
              echo '{"Results":[]}' > results/trivy_sca.json
          elif [ -d project/.venv ]; then
            trivy fs project/.venv --scanners vuln --format json \
              --ignore-unfixed --output results/trivy_sca.json || \
              echo '{"Results":[]}' > results/trivy_sca.json
          else
            echo '{"Results":[]}' > results/trivy_sca.json
          fi


      # ---------------- SBOM - Trivy ----------------
      - name: Trivy SBOM + vulnerabilities + licenses
        run: |
          mkdir -p results
          JARS=$(find project -type f -path "*/target/*.jar" 2>/dev/null)    
          if [ -n "$JARS" ]; then
            trivy fs project --scanners vuln,license \
              --format json --output results/sbom_vuln.json || true
          elif [ -d project/.venv ]; then
            trivy fs project/.venv --scanners vuln,license \
              --format json --output results/sbom_vuln.json || true
          else
            echo '{"Results":[]}' > results/sbom_vuln.json
          fi

      - name: Extract licenses from Trivy SBOM
        run: |
          mkdir -p results
          jq '
            .Results[]?.Licenses[]?.Name
          ' results/sbom_vuln.json 2>/dev/null | \
          sort | uniq -c | \
          awk '{print "\"" $2 "\":" $1}' | \
          jq -s 'add' > results/licenses_tmp.json || \
          echo '{}' > results/licenses_tmp.json


      # ---------------- OSV-Scanner ----------------
      - name: OSV-Scanner (real dependency scan)
        run: |
          mkdir -p results
          JARS=$(find project -type f -path "*/target/*.jar" 2>/dev/null | head -n 20)     
          if [ -n "$JARS" ]; then
            echo "Scanning Java artifacts:"
            echo "$JARS"
            osv-scanner scan $JARS --format json > results/osv-scan.json || \
              echo '{"results":[]}' > results/osv-scan.json
          elif [ -d project/.venv ]; then
            echo "Scanning Python environment"
            osv-scanner scan project/.venv --format json > results/osv-scan.json || \
              echo '{"results":[]}' > results/osv-scan.json
          else
            echo '{"results":[]}' > results/osv-scan.json
          fi





      # ---------------- DEDUPLICATED CVE LISTS BY SEVERITY ----------------
      - name: Generate Severity-Based Deduplicated Files
        run: |
          mkdir -p results
          # --- 1. SCA DEDUPLICATION (By Severity) ---
          for sev in LOW MEDIUM HIGH CRITICAL; do
            case $sev in "MEDIUM") s="med";; "CRITICAL") s="crit";; *) s=$(echo $sev | tr '[:upper:]' '[:lower:]');; esac         
            jq -r --arg sev "$sev" '.Results[]?.Vulnerabilities[]? | select(.Severity==$sev) | .VulnerabilityID' \
              results/trivy_sca.json | sort -u | grep -v '^$' > results/deduped_sca_$s.txt || touch results/deduped_sca_$s.txt
          done
          # --- 2. SBOM DEDUPLICATION (By Severity: Trivy SBOM + OSV) ---
          for sev in LOW MEDIUM HIGH CRITICAL; do
            case $sev in "MEDIUM") s="med";; "CRITICAL") s="crit";; *) s=$(echo $sev | tr '[:upper:]' '[:lower:]');; esac                       
            # A. Get Trivy SBOM IDs
            jq -r --arg sev "$sev" '.Results[]?.Vulnerabilities[]? | select(.Severity==$sev) | .VulnerabilityID' \
              results/sbom_vuln.json > temp_ids.txt || touch temp_ids.txt        
            # B. Get Robust OSV IDs
            jq -r --arg sev "$sev" '
              .results[].packages[].vulnerabilities[]? 
              | select(
                  .database_specific.severity == $sev or 
                  ((.severity[]? | select(.type=="CVSS_V3" or .type=="CVSS_V2") | .score | tonumber) as $s | 
                   if $s < 4 then "LOW" elif $s < 7 then "MEDIUM" elif $s < 9 then "HIGH" else "CRITICAL" end == $sev)
                ) | .id' results/osv-scan.json >> temp_ids.txt || touch temp_ids.txt           
            # C. Save unique IDs to the file the summary expects
            sort -u temp_ids.txt | grep -v '^$' > results/vulnerability_sbom_$s.txt || touch results/vulnerability_sbom_$s.txt
          done
          echo "generated" > results/sbom_status.txt
          rm -f temp_ids.txt
          
      - name: Set artifact name
        id: set_artifact
        run: |
          ARTIFACT_NAME=$(echo "${{ matrix.repo }}" | sed 's#[/:]#_#g')_$(date +%s)
          echo "artifact_name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.set_artifact.outputs.artifact_name }}
          path: results

  # =========================================================
  # GLOBAL SUMMARY
  # =========================================================
  build_summary:
    needs: scan_repo
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: all_results
      - name: Build summary.csv
        run: |
          echo "repo,sca_low,sca_medium,sca_high,sca_critical,sbom_low,sbom_medium,sbom_high,sbom_critical,sbom_status,total" > summary.csv       
          for d in all_results/*; do
            R=$(basename "$d")
            R=${R//_//}
            # -------- 1. DEDUPLICATED SCA COUNTS --------
            # Assuming you created these in the previous job inside the results folder
            SCAL=$(wc -l < "$d/deduped_sca_low.txt" 2>/dev/null || echo 0)
            SCAM=$(wc -l < "$d/deduped_sca_med.txt" 2>/dev/null || echo 0)
            SCAH=$(wc -l < "$d/deduped_sca_high.txt" 2>/dev/null || echo 0)
            SCAC=$(wc -l < "$d/deduped_sca_crit.txt" 2>/dev/null || echo 0)
            # -------- 2. DEDUPLICATED SBOM COUNTS --------
            # These files contain (Trivy SBOM + OSV) sorted and unique
            SB_L=$(wc -l < "$d/vulnerability_sbom_low.txt" 2>/dev/null || echo 0)
            SB_M=$(wc -l < "$d/vulnerability_sbom_med.txt" 2>/dev/null || echo 0)
            SB_H=$(wc -l < "$d/vulnerability_sbom_high.txt" 2>/dev/null || echo 0)
            SB_C=$(wc -l < "$d/vulnerability_sbom_crit.txt" 2>/dev/null || echo 0)
            # -------- 3. STATUS & TOTAL --------
            STATUS=$(cat "$d/sbom_status.txt" 2>/dev/null || echo "generated")
            TOTAL=$((SCAL+SCAM+SCAH+SCAC+SB_L+SB_M+SB_H+SB_C))
            echo "$R,$SCAL,$SCAM,$SCAH,$SCAC,$SB_L,$SB_M,$SB_H,$SB_C,$STATUS,$TOTAL" >> summary.csv
          done
      - uses: actions/upload-artifact@v4
        with:
          name: security-summary
          path: summary.csv
      - name: Build summary_licenses.csv
        run: |
          echo "repo,license_name,count" > summary_licenses.csv
          for d in all_results/*; do
            R=$(basename "$d")
            R=${R//_//}
            if [ -f "$d/licenses_tmp.json" ]; then
              jq -r 'to_entries[] | [.key, .value] | @csv' "$d/licenses_tmp.json" 2>/dev/null | \
              while IFS=',' read -r license count; do
                license=$(echo $license | tr -d '"')
                echo "$R,$license,$count" >> summary_licenses.csv
              done
            fi
          done
      - uses: actions/upload-artifact@v4
        with:
          name: licenses-summary
          path: summary_licenses.csv
